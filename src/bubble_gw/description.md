I think that I don't need this method to receive input the time t, and instead just use the output of self.compute_first_colliding_bubble, which return first colliding bubble of all points if the time is long enough.
Rewrite the method and the bindings according to this requirement.
